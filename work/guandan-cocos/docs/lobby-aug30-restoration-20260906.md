# 恢复 8 月 30 日大厅设计

用户明确否定 9 月 5/6 日的新设计。本轮不继续美化，恢复当时的留白、三张小竖卡、原版透明小鸡商城、标题和账户区样式。

## 版本依据

仓库没有 8 月 30 日新增提交，不能把日期搜索的空结果当作版本丢失。当前会话 2026-08-30 的审查记录记载 LobbyPageDomain 为 504 行；2026-09-02 09:45 UTC 改版前读取了相同的三竖卡源码。该日改版差异的旧 blob 为 `22a2971`，与已保存的 `c5f041c` 提交对应文件一致（504 行）。因此恢复的是 8 月 30 日仍在使用的设计源码，而不是虚构一个 8 月 30 日提交。

恢复文件及 Git blob：

- `LobbyPageDomain.ts`：`22a2971a52e07598c6d3e72207f0e1e9830a52cf`
- `LobbyPlayerProfilePresenter.ts`：`19edb54d7c5aa136fa450d9a49a29eef26ae7cc2`
- `LobbyPageCatalog.ts`：`2b634bcb6b019b02d5f7d35a95042ba606b0787d`

三个文件与历史版本逐字节一致。商城重新引用 `shop-float-chick.png`，玩法重新引用原版三张 JPG。没有用 9 月 5 日备份里的右侧横栏冒充 8 月 30 日设计。

## 保留与归档

不动游戏桌、选牌、计时、联机、机器人、音效、启动修复或生产配置。商城与赛事的当前只读页面和接口边界保留，不因视觉恢复而回退服务逻辑。

改动前备份：`/Users/mac/Documents/Codex/ui-baselines/guandan-before-aug30-design-restore-20260906.tar.gz`。被否定的 `LobbyLandingView` 及测试移动到 `art-source/ui-recovery/rejected/`，使用文本扩展名，不进入运行时代码；并未永久删除。新生成插画仍作为未使用素材保留，不再被大厅引用。

UI/UX 技能仅用于复核入口及边界，不覆盖用户指定的历史设计，尤其不再把原商城图换成通用图标。原版的留白、旧字号及入口分布按原样恢复，不宣称已经完成新的无障碍或低动态模式改造。

## 验收目标

新增恢复测试固定三个历史源码哈希，并检查商城原素材、旧布局以及保留的只读服务入口。网页重建后目视核对三竖卡和透明小鸡商城；本轮不上传体验版、不部署生产。

## 实际验收结果

- `pnpm test` 完整通过，退出码 0；`pnpm verify:architecture` 通过（191 个模块、42 项尺寸预算）。
- Cocos TypeScript `tsc --noEmit` 与 `git diff --check` 通过。
- Cocos Creator 3.8.8 网页构建完成，网页后处理和构建检查通过。
- 浏览器以 874 × 402 核对：右侧三张小竖卡、左下角原版小鸡商城、海岸背景留白恢复；商城进入及返回大厅实测正常，仍为不扣积分的开发中商品预览。
- 本地测试地址：http://localhost:18741/lobby-aug30-restored/ 。最终浏览器停留在恢复后的大厅。
- 本轮仅更新本地网页预览，未重新上传微信体验版或更新生产环境。
