# 大厅局部精修候选：2026-09-06

状态：本地实现及自动／浏览器检查完成，等待用户审美确认。不是新设计版本的全面替换。

## 保留与修改

保留陵水背景、商城小鸡、三张玩法卡原图、位置和尺寸、账户占地、快速开始外框、规则／更多入口及全部留白。未修改通用文字工厂、牌桌、选牌、网络地址、匹配协议或骨骼／粒子资源。

仅改两处运行时源码：

- `LobbyPlayerProfilePresenter.ts`：ID 保持 20 个设计单位的字号，仅取消粗体，把描边减为 1；不靠继续缩字降低主次。
- `LobbyPageDomain.ts`：快速开始改为独立主标题 30／副标题 20，暖金底配深棕文字，加按钮内部细边和顶缘高光；保持外框与点击逻辑不变。赛事开发中标识从 98 收至 90 个设计单位，取消粗重文字描边。移除由本轮替换后不再使用的 `coloredButton` 私有方法。

`ui-ux-pro-max` 的局部检索没有返回适合此棋牌的文字层级条目，因此采用其通用检查表的主次字号、对比、稳定热区要求；没有套用新设计系统或替换美术。

## 视觉判断与修正

先保存并构建修改前版本，再构建 v1，直接截取实际 Cocos 画面。v1 的“快速开始”主次明显，ID／赛事角标不再抢眼，因此保留方向。追加检查发现两行文本布局框交叠 1 个设计单位、按下底色上的副标题对比仅约 3.98:1；最终版分开布局框，并加深副标题，使按下时达到约 4.73:1（主标题约 4.86:1）。不计算描边增益。

最终版与修改前统一为 874×402，来自同一浏览器和账户状态的真实截图。按任一 RGB 通道差值大于 5 统计：10,600 个像素变化，占 3.017%；账户 ID、赛事角标和快速开始三块目标矩形以外为 0。这证明改动范围受控，不证明所有用户必然觉得更美。主观判断：新按钮主次更清楚，二级文字更干净；最终审美接受仍待用户确认。

截图原始 DPR=2，使用 sips 等比缩放为 874×402，没有拼画或修图。小屏截图通过临时调整本地模拟器 CSS 尺寸为 667×375 获得，不冒称手机真机；检查后恢复 CSS 与浏览器尺寸。

## 地址与截图

- 修改前独立预览：http://localhost:18741/lobby-refinement-before-20260906/
- 最终候选：http://localhost:18741/lobby-refinement-20260906/
- 8 月 30 日冻结版不动：http://localhost:18741/lobby-aug30-restored/
- 修改前：`visual-polish-20260906/lobby-refinement-before-874x402.png`
- 最终候选：`visual-polish-20260906/lobby-refinement-final-874x402.png`
- 小横屏：`visual-polish-20260906/lobby-refinement-small.png`

## 验收证据与限制

- 新增默认回归 `tests/lobby-refinement-regression.cjs`，执行当前及冻结版真实大厅渲染方法（模拟 Cocos 节点）：多个宽高／安全边距下，三卡、商城美术与快速开始几何不变，文字不越框，装饰不拦截输入，队列／返回页不变，正常与按下文字对比均不低于 4.5:1。
- 账户回归新增 ID 层级检查；既有多宽度、数据新鲜度、长名称、点击账户等检查保留。
- `pnpm test` 全量、architecture 195 modules／42 budgets、CI 核心和 Creator 两套 TypeScript 检查、`git diff --check`、Web 构建后处理及选牌构建检查通过。
- 日志：`/tmp/guandan-lobby-refinement-final-tests.log`、`/tmp/guandan-lobby-refinement-v2-build.log`。
- 浏览器实测快速开始成功进入本地机器人牌局；申请解散后返回大厅。仅结束本轮测试创建的牌局，没有操作生产牌局。
- 874×402 与 667×375 实际截图检查，未新增布局越界或文字遮挡。没有把这两种桌面模拟器尺寸当成所有手机、系统大字体、屏幕阅读器或深色模式的全面验收；这些不是本轮改变的功能。
- 未重建或上传微信包、未部署服务器。当前手机包仍为上轮滑选修复版；用户确认大厅候选后再同步微信构建。

## 可回退

本轮前的 5 个已有文件备份：

`/Users/mac/Documents/Codex/ui-baselines/guandan-lobby-before-refinement-20260906-1800.tar.gz`

历史补丁 `lobby-refinement-runtime-20260906.patch` 已随工程隔离存入仓库外归档，仅包含本轮两个大厅运行时文件的差异，已通过 `patch --dry-run -R -p1` 反向检查。需要撤回时先核对后续是否修改同一区域，再只反向应用这份差异；不要恢复整个仓库，不要抹掉上轮触控、域名或启动修复。保留记录和对比图，删除／调整本轮新增层级测试的要求即可。

下一步：请用户只比较大厅，不提前扩大到游戏桌、匹配页重做或骨骼动画。
