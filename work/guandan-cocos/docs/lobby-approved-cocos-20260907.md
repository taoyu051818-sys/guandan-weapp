# 大厅模拟稿迁入 Cocos — 2026-09-07

## 设计与实现

以布局实验室候选 02 的最终默认值为基准，未重新设计大厅。874×402 设计坐标通过 `LobbyLayoutPolicy` 等比映射到 Cocos 安全区域；不修改全局 1280×720 设计分辨率，也不改变牌桌。

- 经典 184×222；好友房 188×142；赛事 188×68，保留“筹备中”。
- 账户共用 180×58 半透明底板（40% 不透明度），不再显示 ID 或多个胶囊。
- 商城小鸡 70×70；商城字体 15.4，描边 1.89；图像自高度 50.8 起向下渐隐，文字不参与渐隐。
- 无大厅标题、规则、设置、更多和商品预览副标题。
- 保留原陵水背景和原图。入口图裁掉原图底部文字，用真实 Label 绘制标题与副标题。
- 真实账户／钱包、好友房、经典场和赛事／商城预览导航继续使用现有业务模块。快速开始／继续牌局／正在恢复共用稳定按钮节点。

`LobbyMenuView` 专门负责定稿绘制、裁切和渐隐；新增 SpriteFrame 在节点销毁时释放，未完成加载会取消，不释放共享原始贴图。

## 清理边界

删除旧大厅标题、三等高竖卡绘制、规则／更多入口绑定、商城旧绘制方法、胶囊横排、自造本地账号 ID、无用账户依赖和快捷按钮多余高光。

旧布局算法移至 `tools/lobby-layout-lab/baseline-layout.ts`，只服务原版／候选 01 对照，不进入运行时代码。历史 8 月 30 日源码、旧构建和其他页面仍使用的功能未删除。没有改后端、规则、联机安全策略或生产配置。

## 验证与交付

- Cocos Creator 3.8.8 Web 构建；TypeScript 与架构检查。
- `package.json` 中 65 个回归脚本均正常退出；线上契约、旧资源来源等脚本仍按自身环境条件跳过，不代表完成在线或真机测试。
- 原图裁切、渐隐单调性、精确字号、销毁与加载竞态自动化测试。
- 账户刷新状态、长昵称、积分、头像与个人资料点击、恢复按钮稳定性、匹配路由回归。
- 实际 Cocos 874×402 画面与模拟稿目测对照；实测好友房、商城和返回大厅。
- Web 构建音频审计通过，女声与钢板语音随构建进入资源包。

独立预览：http://localhost:18741/lobby-approved-20260907/

该副本显式使用空业务端点，仅用于本地视觉／单机检查，不访问本地或生产游戏服务；不会自动尝试恢复旧联机牌局。只在该预览副本添加窄面板等比适配样式。正式构建模板和网络配置未改。

未上传微信体验版，未部署服务器，未完成微信真机或在线四人匹配验收。初次未隔离的 Web 预览遇到既有本地服务不可用提示，不能将它算作联机测试通过。
