# 牌桌指示器、自动出牌及结算按钮

- 同花顺指示器在宽屏安全余量内右移最多 20 设计单位，保留工具栏间隔；窄屏不足时不强行挤压。标题浅蓝（184,222,255），底板浅蓝（220,239,255），花色仍按红黑含义与可用/灰色状态显示。
- 普通自动处理不再弹出“操作超时，服务器已自动处理”；真实连续超时进入托管的状态提示保留，权威状态仍刷新。
- 四人经典、转蛋等普通机器人起手等待改为 500–1500ms 随机值；炸弹、拆对子等刻意思考规则保留。小于500ms的显式加速测试时钟仍按比例运行，不改变洗牌/AI随机流。
- 复式桌也在发牌/出牌后的持久快照记录 botWakeAt，使用同一普通范围，tick 仅在时间到达后执行。恢复读取同一个目标时间，不重新抽取；实际执行受服务器 tick 调度精度影响。
- 单副终局按钮为“再来一场”，进入原有场次选择流程；按钮共用 69.6 高度，宽度按最长结算文案预留 361.28，关闭自动换行，赛事返回路径不变。

## 核验

- 客户端默认回归、运行时类型、架构和健康检查通过。覆盖浅蓝样式、单行标签、长文案底板、普通超时无提示及进入托管提示保留。
- 机器人等待范围边界、加速时钟、特殊动作、重启恢复、四人生命周期与私人房机器人、复式双桌运行时和 WebSocket 契约通过。
- Cocos 874×402 浏览器内存夹具检查浅蓝指示器与单行按钮；没有制造线上牌局。夹具清理后恢复正式预览。
- Web 和微信均已重建，微信包校验通过，未上传；仍需真机检查手感。
- 服务端发布 20260910-table-polish，旧版本 20260910-tournament-polish 保留；备份 /srv/guandan/backups/20260910-table-polish/data，两个掼蛋服务正常启动。
