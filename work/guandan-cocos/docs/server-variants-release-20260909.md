# 服务端发布记录：2026-09-09

## 发布范围

- 已按用户授权部署平台服务、牌局服务及重新编译的 shared-core。
- 包含转蛋配置字段、轮换及计分；复式八人双桌、同牌同级、双桌完成屏障、累计结算；当前头像代理修复。
- 本次不上传微信客户端，不替换现有网页，不修改 Nginx、服务凭据及其他业务。
- 不提交或推送当前工作区；其他未提交改动保留。

## 版本与备份

- 主机：`ubuntu@42.193.229.164`
- 当前版本：`/srv/guandan/releases/20260909-room-variants`
- 上一版本：`/srv/guandan/releases/20260909-profile-room-fix`
- 当前入口：`/srv/guandan/current`
- 停服后完整数据备份：`/srv/guandan/backups/20260909-room-variants/data`
- 发布包：`/tmp/guandan-release-20260909-room-variants.tgz`
- SHA-256：`9b3efb421fbdb68aad01fa53f821fe18cff72738ecd9ab945b356d6a9b2e1ea8`
- 部署脚本：`ops/guangzhou/activate-server-variants.sh`

切换前确认牌局服务连接数为 0，无进行中的牌局。两个服务的实际进程工作目录均已指向新版本，状态 active，核验时 NRestarts=0。

## 验证结果

- 本地 shared-core 构建通过；16 个测试文件、82 项测试通过。
- 本地 `npm run test:server` 全部通过。
- 服务器暂存版本通过规则、观战、会话、匹配、头像代理、账户、平台及好友房测试。
- 复式真实八路 WebSocket 隔离集成测试通过：签名入座、房号加入、同牌、隐私隔离、重连及总结果幂等。
- 转蛋 WebSocket 回归通过：两种轮换、3/6 分制、连续两局、重连及最终累计结果。
- 公网平台健康 HTTP 200；匿名资料 HTTP 401；开发登录 HTTP 403。
- 公网 WSS TLS、升级及应用协议通过；非法 Origin 被 HTTP 403 拒绝。
- 原有非掼蛋业务健康 HTTP 200。
- 新旧版本 web 目录 `diff -qr` 无差异。
- 备份及在线数据均为 80 个账户、80 个钱包；数据文件权限保持 600，凭据文件保持 root 所有及 600。未读取或保存登录密码至项目。

测试使用隔离数据和端口；公网检查不创建测试账户或房间。微信真机授权、真机创建转蛋/复式及多人完整对局仍需用户验收，不能以服务端回归替代。

## 回滚注意事项

上一版本和数据备份均已保留。回滚前先检查在线连接、进行中普通牌局及复式牌局，避免中断用户；确认新旧数据兼容性后停两个服务、将 current 原子切回上一版本并重启，再运行公网验证。不要直接覆盖实时数据备份：新版本上线后的用户资料、对局及结算需要先核对和保留。旧版本不支持新增赛制，存在复式或转蛋房间时应先处理兼容和业务状态。
