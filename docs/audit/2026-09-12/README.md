# 全量代码审计长期任务

目标：审查当前第一方全部源码、配置、脚本与测试，并复核关键跨模块链路；交付可追踪的结论和优先级整改表。不以抽查或测试通过冒充全量完成。

实际仓库：`/Users/mac/Documents/Codex/2026-08-02/wo-yi`。启动基线：`1d58999`，包含启动时工作树未提交修改。正式范围以 manifest 的路径和 SHA-256 为准。

调度：已配置当前线程每小时跟进，automation id `automation-2`。执行前必须读取长期目标状态；处于 paused 时只保存已完成进度，不启动新审查或测试，等待用户恢复。旧视觉打磨跟进仍暂停。人工即时执行与调度共享清单，不另开重复项目任务。

## 权限与安全

- 本任务仅审计，不擅自实施业务修复、删除功能、改测试断言、提交推送、部署或重启。
- 只写本目录的审计清单、报告、隔离复现材料；运行安全本地诊断/测试前核对脚本，避免触及真实数据或外部写入。
- 不读取/输出私人配置内容、密钥、SSH密码或真实用户数据；仅核对必要的忽略/注入边界。
- 保留原有五处工作树修改，尤其 RELEASE_ACCEPTANCE_20260910.md 中用户原有改动。

## 覆盖口径

`manifest.json` 包含 Git 跟踪和未忽略的新增文件及哈希；本目录审计工具/报告不计入被审产品。`coverage.json` 仅按批次报告中确实完整阅读审查的文件及匹配哈希计算。

- 第一方代码/配置/脚本/测试逐文件审查。不把静态扫描和测试提及次数算人工覆盖。
- EJS 构建模板、systemd service、nginx conf 和 `.wechatgame` 引擎模板配置也算第一方目标。第二批修正初始分类，将这些 6 文件从资源边界移入源码/配置分母（623 → 629），不是新增产品文件。
- 生成 shared-core 副本单列：对应源文件已审且内容逐字一致才能标记源覆盖同步核验。
- 第三方工具、二进制素材、元数据、文档单列边界；仍需核对入口、许可证、引用和运行包归属，不承诺审阅第三方全部源码或像素。
- ignored 构建输出、node_modules、运行数据不混入源码分母；构建新鲜度、依赖锁定和发布包另有验收门槛。
- 文件变更后既有审阅失效，新源码纳入；报告必须声明当前 HEAD/工作树边界。

## 执行顺序

1. 库存、现有 CI/架构/可达性/同步检查的可信边界。
2. 规则、贡还、升级/定局/转蛋/复式、AI/提示与发牌。
3. 服务器鉴权、消息重放、落座/观战隐私、重连/持久化/房间生命周期。
4. 客户端状态事务、选牌/锁牌/理牌、超时/托管、回调取消及资源释放。
5. 匹配/好友房/八人双桌/赛事/社交与用户资料端到端契约。
6. 视图/音频/动画/资源引用/性能、路由和退役边界。
7. 测试有效性、配置/构建/发布/回滚链路、依赖与包体。

可并行拆分互不重叠文件组；根线程复核问题。每批 JSON 采用 `reviewed:[{path,sha256,notes}]` 与 `findings:[{id,severity,title,path,line,evidence,validation,recommendation}]`；未证实项放独立 concerns 数组。边界专项另用 `boundaryReviewed:[{path,sha256,category,group}]` 与 `boundaryGroups` 说明方法和限度，记录为 boundary-checked；不增加第一方源码覆盖率。

新增/更新批次后，运行 `node docs/audit/2026-09-12/inventory.mjs` 获取补丁，通过 apply_patch 应用以刷新 manifest 与 coverage；再加 `--check` 校验刷新后的完整内容。脚本本身不写文件。严禁手填虚高覆盖率。

## 完成门槛

- 全部第一方目标文件审阅率 100%，发生变化的文件已复审。
- 生成源映射、第三方/资源/历史/忽略目录边界核验完成。
- 关键链路：创建/加入/机器人/准备/发牌/出牌/贡还/结算/下一局/退出/断线恢复/观战；赛事和双桌另外复核。
- 每项确认问题有路径行号、触发条件、影响、验证证据和建议；无法真机验证项明确保留。
- 交付最终总述、优先级整改清单与人工验收表。完成后暂停 automation-2，并将长期目标标记完成。

## 当前状态

2026-09-13，第四十一批及最终审计交付完成：第一方629/629完整审阅，39生成副本同源核验；1167独立边界完成限定核查（文档107、资源308、metadata438、第三方314）。20关键链路已有证据复核，不代表全部通过或全量真人联机。

累计41项确认问题（17 P2 / 24 P3），均未修复。另有vitest/mocker上游公告版本命中，完整在线公告/来源权利/当前新包/真机验证缺口在报告明确保留。原有五处修改和HEAD不变，没有改业务、构建、提交或部署。

入口：[最终报告](FINAL_REPORT.md)、[整改优先级](REMEDIATION_PLAN.md)、[人工验收表](MANUAL_ACCEPTANCE.md)、[跨模块矩阵](CROSS_MODULE_REVIEW.md)。明细仍在STATUS、coverage与batches。

历史automation-2在当前本机配置中不可见，工具view未返回可核原字段，未擅自覆盖或创建重复项。**不能确认旧自动跟进已暂停**；请在定时任务界面核实，详情见STATUS调度收尾限制。目标本身已由目标工具确认为complete；之后任何触发先读取goal，complete不再进行重复审计。
