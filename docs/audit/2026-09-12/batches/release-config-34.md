# 第三十四批：部署配置、依赖锁定与离线资料工具

基线：`1d58999dc6e5455b049e1643660bbba3deee1406`。根线程完整审阅 **19 个新文件，共 1,509 行**；文件清单、SHA-256 和逐项结论见 [JSON](release-config-34.json)。

本批无新增确认缺陷；累计仍为 **40 项（17 P2 / 23 P3）**。原有问题未修复，不能将本批检查通过解读为已可无风险上线。

## 核验结论

- 环境初始化：以内存 FS 执行真实脚本两次，只写一次且保留原值，四个独立合成签名值。加入单独合成微信环境后，两份实际配置加载器均接受；systemd 的 `wechat.env` 正是微信凭据注入位置，没有读取真实文件。
- 离线资料重置：真实纯函数和 CLI 在新建临时目录中操作 50 个合成用户，另跳过机器人和系统用户各 1 个。积分、账本、对局等非资料字段完整保留；纯函数不改原对象，dry-run不写，apply备份/源哈希/权限核验通过；复用备份目录被拒绝且当前文件不再改变。临时目录已清理。
- 依赖锁定：三份 pnpm9 锁分别为 69／1／0 个package，69／1／0 个snapshot；核心86条依赖边闭合，完整性字段格式和manifest声明一致。未下载、安装或升级，不声称无CVE或已完成许可证审查。
- 发布脚本：两份 `bash -n`、四个在访问服务/文件前拒绝的非法参数对照通过；未执行任何发布或服务命令。当前variants入口比旧table-polish入口增加复式活动局门禁、web开关、归档名称白名单等。保留生产凭据，数据备份在停服之后，失败回滚的是代码而不是自动恢复旧数据。
- 发布验收脚本：以全合成fetch／WebSocket／HTTPS运行完整正文，4条HTTP、1条WS协议和1条不可信Origin检查通过；HTTP500、WS非error和Origin被接受三个故障对照被拒绝。真实协议的未知命令不要求数值requestId，因此这里的字符串ID并不是错误原因。
- Cocos配置：设计分辨率、微信横屏、game-assets分包归属一致；cache里局部Spine版本开关不等于开启已禁用的父模块。没有改界面、构建或恢复退役特效。

## 不能由这些结果推出的结论

发布脚本信任操作员审核并核对hash的归档；路径名称白名单不是tar成员类型/软链接目标验证。停止前的连接/活动局检查也不是无竞态的流量排空协议。旧脚本范围更窄，建议后续收敛为单一发布入口，但没有证据认定当前生产正使用旧入口。

代码回滚不能替代新旧版本数据格式兼容性验证；离线资料工具要求先停平台服务，不能当作在线并发事务迁移。发布检查只断言通用错误响应，未验证完整错误码/请求相关性或玩家可开局。真实微信登录、四机牌局、TLS线上状态和源包新鲜度仍待单独验收。

第一次审计VM复用context造成重复声明的探针错误已修正，完整重跑通过；未计为产品缺陷。所有资料/密钥/网络端口均为合成，不涉及真实账户。

## 可重复命令

```sh
node docs/audit/2026-09-12/repro/release-config-34.mjs
```

[探针源码](../repro/release-config-34.mjs)。只写审计目录和自建临时数据；没有修改业务、提交、部署或重启。
