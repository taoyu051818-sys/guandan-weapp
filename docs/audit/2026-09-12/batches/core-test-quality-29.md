# 第二十九批：核心规则与机器人测试有效性

新增完整审阅 **16 文件／2,010 行**：13 个 TypeScript 测试、3 个 CommonJS 回归入口。基线 `1d58999dc6e5455b049e1643660bbba3deee1406`，文件哈希与逐项结论见 [JSON](core-test-quality-29.json)。

本批未新增确认问题；累计仍为39项（17 P2、22 P3）。此前确认的问题不会因为测试通过而自动销项。

## 执行与证据

- 真实 Vitest 4.1.10：13 文件、96 用例通过，禁用结果缓存，单 worker、文件串行；执行当前源码。
- 3 个 CJS 原入口：用隔离转译加载器将 `../dist` 显式指向当前 `src/index.ts`，全部通过；没有改原脚本、编译或覆盖 dist。
- CJS 原生断言执行次数：规则129、贡还21、完整冒烟172（完整冒烟内部再次包含规则/贡还；不得相加当作互不重复测试数量）。
- [内存变异探针](../repro/core-test-quality-29.cjs)：9 个针对性错误均被原断言检测到。

| 故意引入的内存错误 | 原测试的检测信号 |
| --- | --- |
| 同花顺强度从5500降为4500 | 4507 ≠ 5507 |
| 红心配可代16／17 | 应非法的王与配组成对子 |
| 移除锁组硬过滤 | 预期无推荐，却出现拆组单牌 |
| RNG恢复时把状态清零 | 后续数列不一致 |
| 提高首手炸弹优先级 | 首手选择错误变为炸弹 |
| 决策读取其他玩家手牌首项 | 隐藏手牌Proxy抛错 |
| 恢复检查点不清派生缓存 | 预期cache miss，实际cache hit |
| 跳过贡牌旧revision拒绝 | 旧指令错误成功 |
| 双贡第一人选牌事件暴露cardId | 事件精确结构断言失败 |

每个变异先运行未改测试体的正常对照。TypeScript选定测试体使用最小断言适配器；真实96用例另由真实Vitest执行。它不是整个测试集的变异覆盖率，不能将“9/9检出”理解为任意错误都能被检测。

## 审查结论与限制

1. 当前测试确实约束了master唯一策略、隐藏牌面不可读、队伍头游优先、队友冲刺喂牌/保护、锁组、红心配和贡还版本/私密事件，不只是检查函数存在。
2. 同花顺5.5档位在原有未提交测试修改中有双向比较断言，本轮保留该修改原哈希，并验证降回旧档位会被发现。
3. 不洗牌测试核108张物理守恒、每席27张、重放与有限分布报警；当前简单理牌不再承诺六手，不能用发牌测试替代理牌质量或公平性证明。
4. 候选摘要对规则的部分对照共用同一规则实现；7张穷举和节点预算不能证明27张全局最优。固定种子与trace测试也不能证明机器人参数胜率最优。
5. worker.handle测试没有运行真实WebWorker；CJS完整局走兼容API，不能替代网络主链路。完整冒烟初始发牌未固定种子，本轮一次通过不是所有随机牌局终止证明。
6. `tribute-settlement` 中一个标题提到“其他玩家”，正文实际只测正确actor和旧revision；单凭该用例不能声称覆盖错actor。服务端actor边界在此前独立批次验证，不新增没有业务后果证据的缺陷。
7. 旧 `match-format.test.ts` 的错误抗贡事件名（CORE-CONTRACT-06-001）仍未修复；核心动态队伍测试通过也不能排除服务端适配器的RP-13-001，均保留原结论。

## 完成边界

本批后第一方覆盖 **514/629（81.72%）**，剩余115文件。核心源测试已逐文件覆盖，依赖锁文件及其他模块、发布/资源/第三方边界仍未完成。

只有审计材料新增；没有业务或既有测试改动，没有构建、提交、推送、部署或线上访问，五个原有修改保持不变。
