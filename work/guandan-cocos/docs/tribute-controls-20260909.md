# 贡还牌交互与级牌标记核验（2026-09-09）

## 本轮范围

- `CardView`：级字 15 → 23，粗体与 1.2 描边，位置略越出三角斜边；不改变实际牌面。
- 共享规则：红心级牌仅能代表普通点数／级牌，统一候选值并增加防御检查。旧规则已经排除大小王，本轮强化约束与回归，不将其描述为新发现的线上放行漏洞。
- 对子语音：红心配加普通牌，按普通牌实际点数选择已有女声对子音频；非法王替代不播报。两张自然同点数牌仍走原有播报。
- 贡还阶段允许一键理牌与复原；仍保留服务端提交权限、单张贡还选择、托管及请求中限制。
- 新增 `TableTributeInfoView`，锚定对家下方；还牌时正确反转交付方向。移除旧中央贡还提示，结算逻辑保留。

## 验证

- shared-core：16 个测试文件、85 项测试通过，构建通过；37 个客户端生成文件同步核验通过。
- 客户端：完整 79 条回归脚本通过；贡还区域几何测试随后按真实按钮锚点、宽高改为二维矩形判定，单独再次通过。
- TypeScript 运行时与重构测试类型检查通过。
- 架构：226 模块、58 个体积预算通过；健康检查 345 文件通过。
- 浏览器实际 Cocos：27 张双贡手牌，贡牌阶段鼠标点击一键理牌后进入组合排列；切还牌阶段，点击复原成功；对家下方提示与确认按钮分离。
- UI/UX 检查用于文字辨识、触控与矩形遮挡检查。保留用户要求的高牌组自由进入中间：高组与操作区仍可能交叠，没有新增强制布局禁区。
- Web 与微信小游戏均重新构建；小游戏主包 3,107,741 字节（2.96 MiB），资源分包 13,407,404 字节（12.79 MiB），合计 15.75 MiB；发行包核验通过。

## 边界与真机验收

- 本轮未上传微信、未提交 Git、未重启线上服务。共享规则是现有禁止王替代规则的防御性强化，不依赖线上服务重启来恢复该限制。
- 浏览器测试数据仅在页面内存中构造，未发起测试对局网络请求；检查后刷新清除。
- 真机仍需试听红心配组成 2 至 A 对子的声音，并检查进贡／还牌时理牌、单张选择、提交与阶段切换。
- 构建路径：`work/guandan-cocos/build/wechatgame`；Web 预览 `http://localhost:18741/web-desktop/`。
