# 默认资料与头像上传（2026-09-10）

## 交付状态

- 指定范围：QQ 2952180056—2952180105，未扩展范围。
- 成功取得并打包 **24 组**昵称、头像，不是50组。源数据只保留显示昵称、头像文件和内部编号，不保留性别、年龄、地区、邮箱等返回字段。
- UAPI 返回 HTTP429 / VISITOR_DAILY_LIMIT_EXCEEDED，响应明确限制访客30次/天，重置时间2026-09-11T00:00:00+08:00。已停止请求，需API Key或重置后续采。
- 索引9、10、20的头像未通过PNG/JPEG签名检查，13未取得有效昵称；其余未完成索引记录在服务端catalog.json。后续采集脚本可断点继续并遇429立即停止。
- 旧100条纯昵称库已被成对资料库替代，旧文件可从Git恢复。

## 行为

- 首次创建普通平台账号且未提供有效自定义名时，随机分配一组资料并持久化。明确提供的昵称保留；已有账号及编辑过的资料不重置。
- 默认头像在Cocos资源包中；运行时不请求UAPI或QQ域名。匹配机器人和八人房机器人共用服务端资料库，保留isBot/isAI标记，昵称与头像配对。
- 个人资料新增上传头像，仍可编辑昵称或主动授权微信资料。微信使用小游戏chooseImage和离屏canvas；浏览器使用文件选择器。
- 选图仅改变草稿，取消不提交。居中裁剪为256×256 JPEG并重新编码，不上传原照片或EXIF。保存沿用有序队列、账号切换和关闭弹窗隔离。
- 服务端要求登录，校验64KB上限、格式签名和256×256尺寸上限。头像数据仅保存于账号的私有持久字段，不混入公开用户信息、匹配票据或对局快照；替换头像清理旧字节。
- 新生成和显式保存的资料带profileSource，停止旧的静默微信资料覆盖；主动授权按钮仍可使用。

## 验证

- 客户端默认测试、运行时类型检查、架构检查（234模块/58预算）、健康盘点（355文件）通过。
- 新增头像选择隐私/取消/裁剪测试，保存队列选图不提交与取消测试，以及随机资料稳定性、上传校验、HTTP鉴权/读写测试通过。
- 服务端平台回归、普通四人WebSocket回归、八人复式及转蛋回归通过。
- 服务端check:server仍报告既有platform/tournament-service.js 278行超过270行预算；本次未修改赛事模块或放宽该预算。新修改的account-service.js满足240行预算。
- 浏览器已查看资料弹窗：样例001头像和长昵称正常显示，上传与授权按钮无重叠。测试资料只在浏览器内存中，随后刷新清除，未修改线上账号。
- Web和微信生产构建完成；微信包验证：主包3.18MiB，资源分包13.11MiB，总16.29MiB。未上传微信后台。
- 服务端发布20260910-default-profiles，前版20260910-table-polish，启动检查通过，更新前连接数0；备份位于/srv/guandan/backups/20260910-default-profiles/data。

## 待验收

1. 真机点击头像→上传头像→相册选择→预览→修改昵称→保存，重进后保持。
2. 取消选图、取消编辑不改变已保存资料；拒绝隐私/相册权限时显示原因。
3. 新账号随机获得成对资料；四人匹配和复式机器人头像与昵称配对。
4. 补齐剩余来源资料。公开查询可用不等于已取得头像素材的商用授权，正式使用仍需确认相应权利。

## 维护入口

- 服务端采集：work/guandan-windows-source/scripts/import-default-profiles.mjs（只在显式运行时联网）。
- 客户端同步：work/guandan-cocos/scripts/sync-default-profiles.mjs。
- 新职责模块：default-profiles.js、profile-upload.js、ProfileImagePicker.ts、DefaultProfileFrames.ts；未把采集或图像处理塞入牌桌逻辑。
