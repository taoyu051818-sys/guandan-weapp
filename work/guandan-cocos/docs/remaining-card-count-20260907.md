# 其他玩家余牌数量显示阈值

2026-09-07。按用户要求，仅在其他玩家剩余 10 张及以下时显示余牌数量；不是公开其他玩家的牌面。

- 左家、右家、队友剩 11 张及以上：隐藏“剩 N 张”和对应底框，头像与姓名位置不变。
- 剩 10～1 张：显示真实数量；出完后显示头游、二游等名次，不显示“剩 0 张”。
- 本人数量保持显示。自己出完后的只读队友手牌视角不受此数量阈值限制，服务端牌面权限不变。
- 初始／缺失快照不再使用“剩 27 张”占位；新一局、恢复快照按当前数量重新判断，不残留上一局提示。

实现：`TableSnapshotPresenter.projectTableSeatStatus` 统一投影；`TableHudPresenter` 应用于所有座位；`TableHudSeatViewGroup` 同步隐藏空文字与底框。

验证：客户端全量 `pnpm test`、完整 Cocos TypeScript、architecture、HUD 及座位回归通过。新增 27／18／11／10／9／1／0、出完名次、下一局重置和隐藏底框不移动姓名的测试。日志：`/tmp/guandan-card-count-threshold-20260907-tests.log`。

浏览器本地开局实际观察：本人显示 27，另外三家均隐藏。另用仅显示层的样本检查右家 11 张隐藏、队友 10 张显示、左家 1 张显示；未改服务端牌局，测试后已刷新清除样本。该检查不等于真实联机自然出牌至阈值或真机验收。

普通独立预览：http://localhost:18741/card-count-20260907/ 。网页构建与校验通过；微信包在 `build/wechatgame/` 重新构建，并通过 `verify:wechat-build`、`verify:wechat-package`。未上传体验版、未部署服务器；本次数量阈值是客户端修改。上一轮队友牌面授权仍需要服务端同步发布，不能与本次阈值修改混淆。
