# 第三十三批：服务器完整集成测试与断言有效性

基线 HEAD：`1d58999dc6e5455b049e1643660bbba3deee1406`。仅审计，未修改产品或原测试。完整逐行补审 **4 文件 / 2,633 行**，累计 **586/629（93.16%）**。本批无新增确认问题，总计仍为 **40 项（17 P2、23 P3）**。43 个第一方文件及资源/依赖/发布边界仍待完成。

## 阅读与实际运行

| 原测试 | 行数 | 核查重点 | 正常基线 |
| --- | ---: | --- | --- |
| weapp-friend-ticket.smoke.mjs | 720 | 票据过期/撤销世代、踢人替补、开局确认与持久失败、崩溃恢复、token轮换、关闭墓碑 | 前后各通过一次 |
| weapp-terminal-lifecycle.smoke.mjs | 1,094 | 局数/时间/过A终场、普通局准备、拒绝开局、claim掉线及重启/宽限，8个子场景 | 前后各通过一次 |
| weapp-round-state.smoke.mjs | 394 | 超时托管、离线清理、局间准备、双贡/双还私密性及顺序、解散投票、抗贡 | 前后各通过一次 |
| weapp-ws.smoke.mjs | 425 | 四席授权、私有手牌、幂等/版本、连接身份、重连凭证、聊天退役、安全退出 | 前后各通过一次 |

这是四个入口运行两轮，不是八个不同测试套件。实际耗时及逐文件 SHA-256 见同名 JSON。加载当前 shared-core/src，未使用旧 dist 作为规则验证依据；未构建发布包。

## 内存故障注入

使用 [隔离 runner](../repro/server-tests-33.mjs) 和 [精确变异 preload](../repro/server-mutation-preload-33.cjs)，只在当前测试子进程加载时替换一处语句，不改磁盘业务文件或测试断言。记录确认目标实际在 weapp-ws.js 服务进程加载。

| 内存故障 | 对应原测试反馈 | 结论 |
| --- | --- | --- |
| 不再隐藏其他玩家手牌 | “不应向 p1 泄露 p2 手牌”断言失败 | 检出 |
| 双贡未收齐时泄露另一贡者 canonical card ID | “未完成双贡不得通过 canonical state 泄露选择 ID”断言失败 | 检出 |
| 重复请求不再比较正文指纹 | 原测试等待应返回的 error 超时 | 检出，但失败诊断间接 |
| 安全落盘前先广播 roundEnded | 原测试要求广播计数0，实际1 | 检出 |
| 观战型好友房取消已撤销票据检查 | 原测试等待 error/9 超时 | 检出，但失败诊断间接 |

后两种响应等待超时来自原测试3/4秒期限，不是runner的300秒超时。它们能阻止错误通过，但建议今后将“收到非预期成功响应”直接报错，让失败原因更清楚。本批不把测试改进建议计为新的业务缺陷，也不把5/5抽选变异写成全库mutation覆盖率。

## 运行安全与探针修正

所有网络均为回环，状态、outbox和凭证均为本次新建的合成数据。固定端口先验证空闲，只操作自有child/process group，未按端口杀已有服务。延用已审preload32的源重定向与环境/回调/路径边界。

第一次privateHand运行在退出清理处出现kill EPERM，未计入有效结果。检查确认无遗留审计server，临时目录只剩本次变异命中标记；核对后删除该标记和空目录。runner改为先确认进程组仍存在再发送信号，重跑成功。随后四个无故障基线全部再次通过。该清理异常发生于审计工具，不记作产品问题。

最终库存检查通过：586份已审文件哈希全匹配，无重复文件或问题编号；HEAD及五处原有修改哈希不变，git diff --check通过，12个固定测试端口均空闲，无本批临时目录遗留。

## 结论边界

- collector验证真实签名和交付契约，但回执由合成HTTP服务产生，不等于平台账户结算全链路。
- 终场若干场景停服后注入合法阶段快照，不是从27张牌完整打到过A。
- round-state覆盖固定种子/指定单张策略，不能替代全部贡还牌组合、机器人策略或手机/WAN回归。
- 旧env继承和全房版本缓存问题沿用TEST-32-001/QT-32-C01，不重复计数。
- SL-02-001等“终场/待收尾+离线准备”、SP-03-002“关房落盘失败”及其他既有确认项仍有效；正常路径全绿不能抵消对应反例。
- 仅新增审计材料，保留五处用户原修改，无提交、推送、构建、部署或生产重启。

下一批转向剩余工具、构建/发布配置、依赖锁及资源来源，继续按完整文件与哈希计覆盖。
