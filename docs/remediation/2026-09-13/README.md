# 全代码审计整改交付

日期：2026-09-13。范围以 [原审计最终报告](../../audit/2026-09-12/FINAL_REPORT.md) 的 41 项确认缺陷为准：17 项 P2、24 项 P3。本轮完成对应源代码修改和自动回归，**不等于真机或上线验收完成，也不意味着代码库没有其他问题**。

## 完成范围

| 分组 | 数量 | 主要变化 | 逐项记录 |
| --- | ---: | --- | --- |
| 核心规则、客户端状态、构建配置和文档 | 17 | 恢复规则一致性；跨桌版本隔离和本地战绩去重；级牌/同花顺投影；开房设置、资料刷新、资源失败重试 | [core-client.md](core-client.md) |
| 服务端运行时 | 11 | 转蛋机器人采用当前队伍；终场与持久化屏障；可重试关房；投票恢复；复式断连、容量和旧连接清理；延迟一局观战 | [server-runtime.md](server-runtime.md) |
| 平台生命周期 | 4 | 历史参赛成员与当前席位分离；满房校验；观战退出租约；赛事异常桌独立收尾 | [platform.md](platform.md) |
| 客户端交互 | 7 | 过期触摸失效；隐藏比分；换视角与尺寸重排；锁牌高亮；弹窗拦截范围与投票失败重试 | [client-interactions.md](client-interactions.md) |
| 音频和按压动画 | 2 | 可停止的短音频通道；中断真实 Tween，避免快速按压后缩放残留 | [audio-button.md](audio-button.md) |
| **合计** | **41** | 以上分组无重复计数 | |

另外增加 [真实业务消息路由回归](network-authority-route.md)，校验实际入桌、跨桌、旧包拒绝、恢复身份和战绩记账；它不是第 42 项缺陷。

新增职责拆为独立模块：手牌触摸代际 `HandGestureEpoch`、短音频通道 `TransientAudioChannels`、房间关闭协调器 `weapp-room-close-coordinator`、当前参赛成员映射 `match-participants`。既有架构尺寸预算未上调。

UI/UX 技能仅用于触摸取消、失败反馈和尺寸变化的正确性检查；未重画大厅/牌桌，未更改既定视觉风格，未通过关闭现有功能回避问题。

## 自动验证

所有命令在本地工作区运行。涉及 HTTP/WS 的集成使用合成用户、回环监听、隔离临时状态；没有访问线上业务服务。

| 工作目录 | 命令/范围 | 结果 |
| --- | --- | --- |
| `shared-core` | `pnpm check && pnpm test && pnpm build` | 通过；20 个测试文件、125 项测试 |
| `work/guandan-cocos` | `npm test` | 通过；最终登记 98 个回归入口（不是 98 个单独断言） |
| 同上 | `verify:architecture`、`verify:health`、`verify:core-sync` | 通过；241 个客户端模块，368 路径清单、3 个明确非游戏入口；39 份共享生成副本一致 |
| 同上 | `typecheck:runtime`、`typecheck:ci-core`、`typecheck:migration` | 通过 |
| 同上 | `verify:retirement` | 通过；17 个退役运行模块、20 份哈希核验的归档音频，活跃回放/观战保留 |
| `work/guandan-windows-source` | `npm run check:server && npm run test:server` | 通过；87 个生产 JS，37 条尺寸预算；包含平台、WS、重启、机器人、观战、四人/复式和赛事集成链 |
| 仓库根 | `git diff --check`、原有未提交文件 SHA-256 | 通过；原有 5 处文件内容未覆盖 |

音频与按钮新增测试执行本机安装的 Cocos 3.8.8 AudioSource/Tween 实际算法，底层音频与时间使用测试端口；本次不是 SKIP，但不等于真实扬声器试听或 GPU 渲染。其他节点/输入类回归的替身边界见各分报告。

同期按维护者公告将 Vitest 从 4.1.10 修补到 4.1.11，已在新版本完成核心测试；详见 core-client 的依赖维护章节。它不计入 41 项，也不代表全依赖安全审计完成。

## 交付边界

- 修改仍在工作区；未提交、推送 GitHub、更新生产服务器或上传微信。
- 本轮未重新生成 Cocos Web/微信交付包，不能用旧预览或旧体验包验证这些修复；构建启动夹具已改为读取包内真实配置，需在新包生成后运行。
- 未进行手机触摸/后台恢复、微信原生授权/分享/好友关系、真实音频试听或 16 台设备联机验收。
- 历史审计原件未改写为“修复通过”，新的修复和证据单独保存在本目录。外部素材权利链、完整依赖公告与上游来源核验仍不作完成承诺。
- 本地战绩去重是最多 256 条的恢复账本，不替代服务端权威积分，也不承诺无限历史永久去重。

下一步按 [真机与交付验收表](MANUAL_ACCEPTANCE.md) 核验新构建；该表所有人工状态暂为待验收。
