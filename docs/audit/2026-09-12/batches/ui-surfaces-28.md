# 第二十八批：大厅、加载页、座位与结算视图

基线 `1d58999dc6e5455b049e1643660bbba3deee1406`。新增完整审阅 **14 文件／1,380 行**（6 UI 源文件、8 测试）；辅助重读不重复计数。逐文件 SHA-256、说明和运行输出见 [JSON](ui-surfaces-28.json)。

## 确认问题

### LP-28-001 · P3：赛事入口仍标“筹备中”

- 位置：`LobbyMenuView.ts:73`。
- 触发：平台返回合法、开放的免费 16 人赛事。
- 实际链路：大厅卡片 → `LobbyPageDomain` → `TournamentCenterController` → “免费报名” → “确认检录”，但卡片始终显示“筹备中”。
- 影响：入口状态误导用户；不阻止实际报名，不是服务端积分或赛程错误。
- 既有测试 `lobby-aug30-restore-regression.cjs:34/37` 同时要求真实赛事入口与旧文案，因此测试通过没有排除此问题。
- 建议：入口使用“16 人积分赛”等中性描述，或与赛事详情共用可用性模型；素材/布局保护与业务文案断言分离。
- 边界：使用合法合成 API 响应，没有访问线上，也不声称线上此刻必有开放赛事。实际页面绘图/GPU未验收。

## 安全验证

命令：`node docs/audit/2026-09-12/repro/ui-surfaces-28.cjs`。

| 检查 | 验证范围 | 结果 |
| --- | --- | --- |
| 大厅几何 | 120 组宽高与非对称安全区，六区域比例/间距/边界 | 通过 |
| 素材裁切与释放 | 36 组，468 切片释放，40 个取消后迟到回调 | 无新增切片，计时器归零 |
| 加载反馈 | 6 组背景/viewport；进度越界、4 错误码、多次点击、淡出与销毁 | 通过 |
| 实际启动协调器 | 6 组成功、Bundle/素材/牌面/初始化失败、销毁后迟到 | 重试与初始化一次性守卫通过 |
| 座位状态 | 120 组队伍/视角/张数/名次；离线恢复代际和回合 Tween | 通过 |
| 结算复用 | 8 组尺寸/单双行、160 次相同内容更新 | 节点稳定，变化重建与重复清除通过 |
| 赛事入口 | 实际大厅点击与赛事 controller/model，合成免费报名接口 | 复现 LP-28-001 |

8 个新增审阅的既有测试均通过；已审 `ui-recovery-regression.cjs` 重读重跑也通过，不新增覆盖。实际 Cocos 3.8.8 九份 Tween/Action 算法加载路径和哈希已记录；其他节点/绘图、Bundle、API、排程及重启端口均为内存替身，不是完整 Cocos 或微信真机执行。

最初探针使用严格浮点相等/绝对非负断言，遇到比例舍入及最小 `-2.842170943040401e-14` 的坐标舍入。调整审计探针的数值容差后通过，**不是产品缺陷或产品代码修复**。40 个取消告警属于取消完成与替身销毁顺序，不据此报告泄漏。

## 架构与退役判断

`PlayerSeatController` 的旧文字/面板虽被主 HUD 隐藏，仍承担连接状态与出牌原点；主协调器仍调用它，不能把整个组件认定为僵尸代码。结算表面采用内容键复用，加载页由启动 owner 控制初始化与重试；本批没有确认新的职责拆分缺陷。

UI/UX 技能用于加载错误恢复与下一步反馈检查，未改布局、样式或引入不适合 Cocos 的 DOM/ARIA 要求。

## 边界

只新增审计材料，保留五个原有修改；没有改业务、既有测试、构建输出，没有提交、推送或部署。本批使第一方覆盖达到 **498 / 629（79.17%）**，累计确认 **39 项（17 P2、22 P3）**。剩余 131 文件及跨模块、发布/依赖/资源边界仍待审，不能称全量完成。
