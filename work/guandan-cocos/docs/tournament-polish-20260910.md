# 赛事中心微调与取消报名

- 奖杯原文件实际为蓝灰实底；按用户许可本地去底，保留金色与青色造型及原素材。新增 512×512 RGBA 透明副本（229,449 字节），按 244×244 等比例显示，未改变大厅原素材引用。可复现处理脚本位于 art-source/ui/tournament-cutout.py，不进入游戏包。
- 16 人积分赛标题使用统一小圆角底板；赛况、排名、规则以内容面板中心居中。
- 删除刷新按钮。页面活动期间每次请求结束后间隔 1 秒自动同步，包括失败重试及终局；不重叠请求，数据不变不重建节点，后台/离开后旧回调不生效。点击遇到静默刷新时等待完成再重新核对状态。
- 新增取消报名：免费 fixed16-latin-3 赛事、名单锁定前可用，已检录也能退；同时删除报名、释放检录名额、移除零场次排名。开赛后由服务器拒绝，不支持收费赛事退费。
- 服务端事务和幂等回执防止重复取消或旧请求取消重新报名；旧报名请求不能恢复已取消的报名。重新报名使用新的幂等键。

## 验证与交付

- 客户端默认回归全部通过；额外覆盖取消双击、重新报名、断网自动恢复、离页后排队点击失效、取消接口方法与幂等头。
- 类型检查、架构与健康检查通过。服务端取消事务、HTTP 接口、名单锁定、三轮完整赛制契约及部署脚本中的全套回归通过。
- Cocos 874×402 预览检查透明奖杯、居中页签、标题底板、已检录及取消按钮。预览夹具仅在浏览器内存使用，未写生产报名数据。
- 微信构建验证通过：主包 3,327,675 字节（3.17 MiB），资源分包 13,637,135 字节（13.01 MiB），总计 16,964,810 字节（16.18 MiB）。未上传微信后台。
- 服务端部署为 20260910-tournament-polish；按用户明确指示允许打断活动连接，仅重启掼蛋两个服务，没有重启主机。旧版本保留，数据备份位于 /srv/guandan/backups/20260910-tournament-polish/data。
- 没有执行真实 16 台手机赛事，仍需新微信包进行真机验收。
