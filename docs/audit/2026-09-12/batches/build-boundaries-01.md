# 构建与检查边界首批

完整审查 14 个第一方代码/配置/测试文件；逐文件哈希和检查结论见同名 JSON。未确认新的业务缺陷，不为数量强行添加问题。

已验证：客户端完整 `test:ci`、服务端静态检查和完整 `test:server`、产物哈希清单测试、四类临时合成存储恢复均通过。实际 Cocos 编译、渲染、手机性能和线上恢复尚不属于本轮这些命令的证明范围。

重点边界：默认 CI 的纯逻辑类型检查不是完整 Cocos 类型检查；静态可达性不是实际包体可达性；测试提及不是覆盖率；release manifest 不是可复现构建证明。后续完成报告必须保留这些验收门槛。

本批仅写审计报告，不修改产品或生产配置。
