# 2026-09-09 牌桌操作修正

## 本轮范围

- 四人房明确进入托管后调用现有 master bot 策略；复式自动行动同样复用该策略并保存 AI checkpoint。首个超时尚未成为托管时仍遵循原有超时处理。
- 理牌的锁定仅保护排列；底牌用于整组选牌，顶部/中间识别条可独立取消。选择事件携带目标布尔值，避免重复拖动反向切换。
- 操作栏不再用整块透明矩形阻断手牌命中，只拦截真实按钮和计时器。
- TableButtonMetrics 统一操作高度 69.6、字号 33.6 设计单位，按钮文字宽度自适应；TableToolbarLayout 独立负责工具栏尺寸及基线。
- 快捷语控制器、策略、面板、气泡、收发消息、菜单配置已退役。旧房间协议中的聊天设置字段仅保留兼容读取，不再提供功能。唯一快捷语音频及 meta 移至 asset-library/retired-audio/quick-chat，保留哈希清单。
- 同花顺理牌栏采用浅色底，保留花色灰态和选择反馈。

## 验证

- 78 个客户端回归模块通过；运行时类型检查、架构检查（225 模块／58 项大小预算）、健康检查、37 个核心源文件同步检查通过。
- 服务端全套测试通过；正式服务器暂存版本额外通过托管策略路由、四人机器人开局/结算、八人双桌、转蛋、账户与签名结算测试。
- 874 × 402 Web 实际画布、隔离内存牌局：27 张手牌，一键理牌后点击八炸底牌选中 8 张；点击顶部牌后剩 7 张选中，顶牌恢复正常。
- 实际鼠标点击托管按钮触发 set，收到模拟状态后显示取消托管，再点击触发 cancel；这项检查隔离网络，不冒充真机联机验收。
- 微信正式包核验通过：主包 3,105,134 字节（2.96 MiB），game-assets 13,407,404 字节（12.79 MiB），总计 15.75 MiB。未上传微信平台。
- 服务端部署 20260909-table-controls，上一版 20260909-friend-bot-buttons；数据备份 /srv/guandan/backups/20260909-table-controls/data。生产 Web 文件原样保留。HTTPS、WSS、登录边界检查通过。

## 真机验收

重新编译 build/wechatgame 后，验证四人/复式托管的实际出牌、取消托管恢复手动、高叠牌顶牌点选与滑动取消。Web 鼠标验证不能替代微信真机触控验收。
