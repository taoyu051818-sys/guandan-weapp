# 第三十六批：素材工具、历史文档生成器与默认资料目录

基线 HEAD：`1d58999dc6e5455b049e1643660bbba3deee1406`。2026-09-13，root。新增完整审阅8文件、1,403行。本批无新增确认问题，累计仍为40项（17 P2 / 23 P3），均未修复。

## 审阅结论

- 程序素材来源可复现：6张炸弹纹理固定按名种子，内存执行实际生成及保存边界后，与仓库PNG字节和像素均一致。16帧大厅闪光图同样一致（384×96，3,786字节），独立CRC校验通过。该脚本不控制播放间隔，不能由历史文档的10秒推断当前效果。
- 两个抠图工具属于美术离线工具。计时器脚本通过边界连通排除背景，合成内部白色保留；预乘alpha缩放、空图拒绝通过。奖杯脚本无main保护，会直接覆盖固定衍生文件，本批未执行，只读核现有透明图。色差阈值专用于原奖杯，不承诺通用抠图。
- “钢板”处理脚本通过4个纯合成WAV场景：输出mono PCM16、淡入淡出/时长/限幅、拒绝已有目标且保持双方文件、拒绝静音、拒绝立体声。未调用外部TTS或替换实际语音。
- 默认资料实际24组成对昵称头像，不是50组；26个失败/未取索引与成功索引互斥且合计1..50。24份服务器图片与客户端镜像哈希一致，原同步脚本在截留写端口后输出的TS也与现有文件完全一致。既有交付说明已如实记录访客限额和失败，不新增“缺26导致运行失败”的误报。

## 历史与授权边界

生成DOCX的源文件完整审閱，语法和8份截图输入存在性通过；没有调用build或写出新DOCX。其2026-08-04固定文案含旧金边/光环、旧本地端点与旧“P0”结论，仅为历史报告，不作为当前缺陷证据，也不是重启退役效果的指令。

图像提示词是生成输入记录；外部头像可访问、edge-tts开源许可证，都不能单独证明对应图像/声音的商业使用权。完整来源及第三方边界仍另行核查。本批未联网补采。

## 可复制验证

- [JS隔离探针](../repro/asset-tools-36.mjs)：原glint及资料同步脚本，只捕获输出，不写产品目录。
- [Python隔离探针](../repro/asset-tools-36.py)：原图元/计时器函数；最终图片save捕获为内存，音频只用自有临时合成输入。
- [逐文件哈希与结果](asset-tools-36.json)。

首次探针把浮点镜像取整的1级alpha差、极低alpha预乘8位量化当成严格相等，属于探针假设过强；修正容差后完整重跑通过，未记产品问题。实际生成图字节一致验证仍为精确比较。

本批不证明真机听感/视觉、源包新鲜度、完整许可或全库健康；仅写审计目录，无产品代码、原测试、头像、音频和素材变更。
